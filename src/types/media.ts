export enum Media {
	Speech = "speech",
	Image = "image",
	Video = "video",
	Audio = "audio",
}

export enum MediaOutputSubtype {
	Screen = "screen",
	Zoom = "zoom",
}

export enum MediaOutputFinalType {
	Stitched = "stitched",
}